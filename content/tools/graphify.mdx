---
title: Graphify
type: tool
status: draft
source: Chase AI
sourceUrl: https://github.com/chaseai/graphify
license: unverified
verifiedOn: 1970-01-01
depth: showcase
situation: Turning session notes and documents into a queryable knowledge graph.
---

Chase AI's Graphify, which we adapted for Windows.

R21 maintains the Windows adaptation — the skill in our workspace declares
`name: graphify-windows`. The upstream tool is Chase AI's work; the adaptation
is ours, and both halves of that sentence matter.

> **This entry is deliberately `status: draft` and cannot be published as-is.**
> `license` is `unverified` and `verifiedOn` is the epoch, so it will fail the
> attribution guard and the staleness guard the moment Phase 2 lands. That is
> the point: it is the fixture those guards get tested against.
>
> Two claims must be checked against the registry before this goes live, per
> spec §7 — the canonical repo URL above (a guess until the GitHub API confirms
> it) and the licence. The "~60K stars" figure circulating in our wiki is
> **unverified** and must not be published without a live fetch.
